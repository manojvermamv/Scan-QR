<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- not selected color -->
    <item android:color="@color/buttonfontcolor_disabled" android:state_selected="false" />

    <!-- selected color -->
    <item android:color="@color/buttonfontcolor_enabled" android:state_selected="true" />

    <!-- default color -->
    <item android:color="@color/buttonfontcolor_enabled" />

</selector>